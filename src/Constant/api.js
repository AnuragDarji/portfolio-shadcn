export const BASE_URL = "https://contact-form-gilt-three.vercel.app";

const API_ENDPOINTS = {
  CONTECT_FORM: `${BASE_URL}/api/contact`,
  PROJECTS: `${BASE_URL}/api/projects`,
};

export default API_ENDPOINTS;
